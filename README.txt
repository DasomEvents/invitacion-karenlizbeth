Coloca aquí tu archivo de música con el nombre exacto:

    party.mp3

Ruta final que debe quedar:  audio/party.mp3

La página ya está preparada para reproducirlo automáticamente desde ese
nombre y ruta. No es necesario tocar el código HTML, CSS ni JS: solo
sube el archivo party.mp3 dentro de esta carpeta "audio".

Este archivo README.txt es solo un marcador para que la carpeta "audio"
no quede vacía al subirla a GitHub (Git no sube carpetas vacías). Puedes
dejarlo, no afecta el funcionamiento del sitio.
